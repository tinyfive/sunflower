<div class="page-header">
  <%= link_to t('.new_apply'), new_apply_path, class: 'btn btn-success pull-right' %>
  <h1>申请列表</h1>
</div>

<%= search_form_for @q do |f| %>
  <%= f.label :patient_name_or_patient_social_relations_name_cont %>
  <%= f.search_field :patient_name_or_patient_social_relations_name_cont %>
  <%= f.submit %>
<% end %>

<table class="table table-hover table-striped">
  <thead>
    <tr>
      <% %i[name].each do |field| %>
        <th><%= Patient.human_attribute_name field %></th>
      <% end %>
      <% %i[hospital estimate_discharge_at].each do |field| %>
        <th><%= Apply.human_attribute_name field %></th>
      <% end %>
      <th class="text-right"></th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: 'apply', collection: @applies %>
  </tbody>
</table>
